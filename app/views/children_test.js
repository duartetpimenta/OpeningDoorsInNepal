'use strict';

describe('myApp.children module', function() {

  beforeEach(module('myApp.children'));

  describe('children controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var childrenCtrl = $controller('ChildrenCtrl');
      expect(childrenCtrl).toBeDefined();
    }));

  });
});